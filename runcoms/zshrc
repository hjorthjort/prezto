#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Spacemacs uses the default shell, so the last clause protects
# against annoying error messages.
if [[ "$TMUX" = "" && "$(tty)" != "/dev/tty1" && "$(tty)" != "/dev/tty2" && "$TERM" != "screen" && "$TERM" != "dumb" && $INSIDE_EMACS == "" ]]; then
    exec tmux
fi

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

#Set unlimited history
export HISTFILESIZE=
export HISTTIMEFORMAT="[%F %T] "

#Plugins
plugins=(pacman tmux haskell fasd colored-man sudo git)

# opam configuration
test -r /home/hjort/.opam/opam-init/init.zsh && . /home/hjort/.opam/opam-init/init.zsh > /dev/null 2> /dev/null || true
